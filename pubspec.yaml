name: todoapp
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  system_theme: ^2.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: '^2.0.3'
  msix: ^3.16.1

flutter:
  uses-material-design: true

msix_config:
  display_name: Todo
  publisher_display_name: BloomDevelop
  identity_name: BloomDevelop.apps.todoapp
  msix_version: 1.0.0.0
  logo_path: Images/Icon.png
  capabilities: internetClient,
